---
import Layout from '../layouts/Layout.astro';

const lessons = [
  { id: 1, title: 'Luật cơ bản cờ vua', level: 'Cơ bản', duration: '30 phút' },
  { id: 2, title: 'Các nước đi của quân cờ', level: 'Cơ bản', duration: '45 phút' },
  { id: 3, title: 'Chiến thuật cơ bản', level: 'Trung cấp', duration: '60 phút' },
  { id: 4, title: 'Khai cuộc phổ biến', level: 'Trung cấp', duration: '90 phút' },
  { id: 5, title: 'Chiến lược tàn cuộc', level: 'Nâng cao', duration: '120 phút' },
];
---

<Layout title="Bài học - Website Cờ Vua">
  <main class="lessons-page">
    <h1>Bài học cờ vua</h1>
    <p class="intro">Khám phá các bài học cờ vua từ cơ bản đến nâng cao. Nâng cao kỹ năng của bạn và trở thành một kỳ thủ giỏi hơn!</p>
    
    <div class="lesson-filters">
      <button class="filter-btn active">Tất cả</button>
      <button class="filter-btn">Cơ bản</button>
      <button class="filter-btn">Trung cấp</button>
      <button class="filter-btn">Nâng cao</button>
    </div>

    <div class="lesson-grid">
      {lessons.map((lesson) => (
        <div class="lesson-card">
          <h3>{lesson.title}</h3>
          <p class="lesson-info">
            <span class="lesson-level">{lesson.level}</span>
            <span class="lesson-duration">{lesson.duration}</span>
          </p>
          <a href={`/lessons/lesson-${lesson.id}/`} class="lesson-link">Bắt đầu học</a>
        </div>
      ))}
    </div>
  </main>
</Layout>

<style>
  .lessons-page {
    max-width: 1200px;
    margin: 0 auto;
    padding: 2rem;
  }

  h1 {
    font-size: 2.5rem;
    color: #333;
    margin-bottom: 1rem;
  }

  .intro {
    font-size: 1.1rem;
    color: #666;
    margin-bottom: 2rem;
  }

  .lesson-filters {
    display: flex;
    justify-content: center;
    gap: 1rem;
    margin-bottom: 2rem;
  }

  .filter-btn {
    background-color: #f0f0f0;
    border: none;
    padding: 0.5rem 1rem;
    border-radius: 20px;
    cursor: pointer;
    transition: background-color 0.3s;
  }

  .filter-btn:hover, .filter-btn.active {
    background-color: #333;
    color: white;
  }

  .lesson-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
    gap: 2rem;
  }

  .lesson-card {
    background-color: white;
    border-radius: 10px;
    padding: 1.5rem;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    transition: transform 0.3s, box-shadow 0.3s;
  }

  .lesson-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 6px 8px rgba(0, 0, 0, 0.15);
  }

  .lesson-card h3 {
    font-size: 1.2rem;
    color: #333;
    margin-bottom: 0.5rem;
  }

  .lesson-info {
    display: flex;
    justify-content: space-between;
    margin-bottom: 1rem;
    font-size: 0.9rem;
    color: #666;
  }

  .lesson-link {
    display: inline-block;
    background-color: #4a4a4a;
    color: white;
    padding: 0.5rem 1rem;
    border-radius: 5px;
    text-decoration: none;
    transition: background-color 0.3s;
  }

  .lesson-link:hover {
    background-color: #333;
  }
</style>
